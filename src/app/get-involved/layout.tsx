import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Get Involved",
  description: "Support The Wild Card Project. Donate via Paystack or Flutterwave, mobile money, or bank transfer.",
};

export default function GetInvolvedLayout({ children }: { children: React.ReactNode }) {
  return children;
}
